﻿@if (TempData.ContainsKey("message"))
{

    PTDU.Library.XMessage message = (PTDU.Library.XMessage)TempData["message"];
    <div class="alert alert-@message.TypeMsg">
        @message.Msg
    </div>
}